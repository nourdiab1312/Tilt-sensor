int reading=0;
int b=0;
void setup()
{
  Serial.begin(9600);
  pinMode(13, OUTPUT);
   pinMode(A0, INPUT);
  pinMode(11, INPUT);
}

void loop()
{
reading =analogRead(A0);
  Serial.println(b);
  b=map(b,0,1023,0,255);
  delay(1000);
}
